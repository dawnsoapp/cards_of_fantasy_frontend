class storyPrompt {
    constructor(speaker, sprite, text, next, option) {
        this.speaker == speaker;
        this.sprite == sprite;
        this.text == text;
        this.next == next;
        this.option == option;
    }
}

intro1 == storyPrompt(
    speaker == "dawn", text == "Hi there", next == intro2, option == ""
    )